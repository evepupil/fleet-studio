# 服务层 · 调度引擎与装配

> 模块定位：派活 → 排队 → 放行 → 跟踪 → 收尾的全流程，以及把配置、存储、进程托管、引擎、接口装配成一个常驻服务 · 对应代码：`packages/daemon/src/engine/`、`packages/daemon/src/app/`、`packages/daemon/src/config/`、`packages/daemon/src/main.ts` · 所属里程碑：[M1 闸门服务与 fleet 命令](../roadmap.md#m1)、[M5 第二版底座](../roadmap.md#m5)（过期清理在 [M3](../roadmap.md#m3) 验收）· 状态：已完成（第二版已实现）· 最近更新：2026-09-26

## 1. 职责与边界

- **管：** 实现 `FleetService`（`engine/service.ts`）；放行循环；每次运行的跟踪与收尾；超时；取消；续接；等待；时间线；快照；重启接管；过期清理；配置热更新；服务启动与退出。
- **不管：** 规则本身（全部调用 `@fleet/core`）；SQL（存储层）；操作系统细节（进程托管层）；HTTP（接口层）。
- 依赖的契约：`store/types.ts`、`process/types.ts`、`app/types.ts`、`engine/service.ts`。

## 2. 结构

| 文件 | 内容 | 哪一路写 |
|---|---|---|
| `app/types.ts` | 契约：`Logger`、`DaemonPaths`、`ConfigStore`、`DaemonInfo`（主控已写好） | — |
| `app/paths.ts` | `resolveDaemonPaths(home): DaemonPaths` | 装配路 |
| `app/logger.ts` | `createFileLogger(file, options?)`：追加写，超过 5MB 把旧文件改名为 `.1` 再写新的 | 装配路 |
| `app/daemonInfo.ts` | `readDaemonInfo`、`writeDaemonInfo`、`removeDaemonInfoIfOwned` | 装配路 |
| `config/configStore.ts` | `openConfigStore(file, logger): ConfigStore` | 装配路 |
| `app/startDaemon.ts` | `startDaemon(options): Promise<DaemonHandle>` | 装配路 |
| `main.ts` | 进程入口 | 装配路 |
| `engine/engine.ts` | `createEngine(deps): Engine`，组装下面各部分 | 引擎路 |
| `engine/events.ts` | 事件总线 | 引擎路 |
| `engine/submit.ts` | 派活与续接的校验和建档 | 引擎路 |
| `engine/dispatcher.ts` | 放行循环 | 引擎路 |
| `engine/launcher.ts` | 把一次运行拉起来 | 引擎路 |
| `engine/runTracker.ts` | 跟踪在跑的运行：读输出、解析、落库节流、收尾触发 | 引擎路 |
| `engine/finisher.ts` | 判定结局、写库、通知 | 引擎路 |
| `engine/timeouts.ts` | 超时与「收尾后迟迟不退出」检查 | 引擎路 |
| `engine/timelineStore.ts` | 每个苦工的时间线缓存、落盘、增量通知 | 引擎路 |
| `engine/snapshotService.ts` | 查库、算排队位置、调用 `buildSnapshot`、缓存 | 引擎路 |
| `engine/waiter.ts` | 等待苦工结束 | 引擎路 |
| `engine/recovery.ts` | 服务启动时接管 | 引擎路 |
| `engine/retention.ts` | 过期清理 | 引擎路 |
| `engine/identity.ts` | `identityOfRun(运行)`：从运行记录拼出核对进程身份用的信息（3.6 末尾） | 引擎路 |

## 3. 引擎规格

### 3.1 `createEngine(deps)`

```ts
interface EngineDeps {
  repos: Repos;
  host: ProcessHost;
  config: ConfigStore;
  paths: DaemonPaths;
  logger: Logger;
  version: string;
  startedAt: string;
  getPort: () => number;
  platform: "win32" | "posix";
  /** 展开 ~ 用 */
  homeDir: string;
  /** 展开 builtin: 用，即 fleet-studio 仓库根目录 */
  builtinRoot: string;
  /** 默认 Date.now，测试可注入 */
  now?: () => number;
  /** HTTP 收到退出请求后调用 */
  onShutdownRequested: () => void;
}
interface Engine extends FleetService {
  /** 接管 → 过期清理 → 启动定时器 → 放行一次 */
  start(): Promise<void>;
  /** 停掉定时器、放弃跟踪；苦工进程继续运行，等下次启动接管 */
  stop(): Promise<void>;
}
```

### 3.2 派活 `submit(request)`

1. 取配置：池 = `request.pool ?? defaults.pool`，角色 = `request.role ?? defaults.role`，运行时 = `request.runtime ?? defaults.runtime`。池、角色不存在 → `invalid_request`；`getRuntimeAdapter(运行时).displayModel(池)` 为 null → `invalid_request`「池 <id> 没有为 <运行时> 指定模型」。
2. 路径：`normalizeProjectPath(projectPath, platform)` 和 `normalizeProjectPath(cwd, platform)`；工作目录必须存在且是目录，否则 `invalid_request`「工作目录不存在：<cwd>」。
3. 项目：按 `projectKeyOf` 取；没有就新建，`colorIndex = pickColorIndex(projects.colorIndicesInUse(7 天前), key)`，`name = projectNameOf(path)`。
4. 苦工：编号用 `createWorkerId()`，撞了重试；标题 = `request.title ?? 正文第一个非空行压成一行截到 40 字`；`model = displayModel`；`thinking = request.thinking ?? defaults.thinking`；pi 的 `sessionRef = piSessionIdOf(编号)`，opencode 为 null；`latestRunSeq = 1`。
5. 第 1 次运行：排队中，`timeoutMs = resolveRunTimeoutMs(config, 池, request.timeoutMin)`，`queueTimeoutMs = resolveQueueTimeoutMs(config, 池, request.queueTimeoutMin)`，用量为零，`eventCount = 0`。
6. 项目、苦工、运行在一个事务里写入。之后刷新时间线（出现 `run_start`）、发事件、请求放行，返回摘要。

### 3.3 续接 `send(id, request)`

苦工不存在 → `not_found`；最新运行不是终态 → `conflict`「苦工还在排队或工作中，等它结束后再追加指令」；opencode 且 `sessionRef` 为 null → `conflict`「会话还没建立，无法续接」。新建第 `latestRunSeq + 1` 次运行（超时按苦工所在池和 `request.timeoutMin` 算，排队超时取池和全局默认），更新 `latestRunSeq`，同样写事务、刷新时间线、发事件、请求放行。

### 3.4 放行循环

- `requestDispatch()`：用 `setImmediate` 合并短时间内的多次请求；同一时刻只跑一轮。另有每秒一次的兜底。
- 一轮：
  1. 读活跃运行和它们的苦工。
  2. 排队中、但苦工所在的池已不在配置里的运行 → 失败 `pool_removed`，说明「所在的池 <id> 已从配置中删除」。
  3. 上限取配置里各池；在跑列表 = 状态为工作中的运行（含正在启动的）；调用 `planDispatch`。
  4. 对每个要放行的运行：**先**把状态改成工作中、写 `startedAt`（占住槽位，防止下一轮重复放行），再交给启动器。

### 3.5 启动 `launcher`

1. 角色：配置里找不到（被删了）就用只有编号的空角色，记一条警告。`expandRolePaths(角色, { home: homeDir, builtinRoot })`。
2. opencode 且 `opencodePromptPath(角色)` 非 null：读这个文件作为 `rolePromptText`；读不到 → 该运行失败 `spawn_error`「读不到角色提示词：<路径>」。
3. `adapter.buildLaunch({ prompt: 运行.prompt, runDir: paths.runDir(运行编号), cwd, title, sessionRef, isContinuation: seq > 1, thinking, pool, role, rolePromptText })`。
4. `host.resolve(运行时)`，`host.workerEnv()`，`host.spawn({ command, args, cwd, env, stdoutPath: outFile, stderrPath: errFile, files })`。
5. 成功：一次写入 `pid`、`processImage`、`spawnedAt = now()`（`spawn` 返回之后立刻取时间，真正的进程创建时间一定不晚于它，之后核对进程号有没有被复用全靠它），建跟踪器，注册 `onExit`。
6. 任何一步抛错：该运行失败 `spawn_error`，说明取错误信息；请求放行（槽位已空出）。
7. 启动期间被取消（见 3.9）：拿到进程号后立刻结束它。

### 3.6 跟踪 `runTracker`

引擎用一个 400 毫秒的定时器轮询所有跟踪器（不要每个跟踪器一个定时器）。每个跟踪器每次：

1. 依次读 stdout、stderr 文件的新行，逐行交给解析器（`stream` 分别为 `stdout`、`stderr`，`at` 为当前时间）。
2. 新产出的草稿追加到 `timeline.jsonl`（每行一个草稿 JSON），并交给时间线缓存（3.10）。
3. 进展有变化时落库（`usage`、`retry`、`activity`、`lastActivityAt = progress.lastEventAt`、`finalText`、`eventCount`），同一运行最多每秒一次；阶段、重试信息、结局发生变化时立即落库。落库后发 `worker` 和 `snapshot` 事件。
4. opencode：`progress.sessionRef` 非 null 而苦工的 `sessionRef` 还是 null → 立即写入苦工（这样即使本次运行失败，也能续接）。
5. `progress.phase === "ended"` 后 10 秒进程仍未退出 → `host.kill(pid, 身份)`（`killedBy` 保持 null，结局以事件流为准）。
6. 进程退出（`onExit` 触发，或接管的进程在 2 秒一次的存活探测 `host.isAlive(pid, 身份)` 里发现已不在）→ 把两份文件读完、`flush` 出最后半行也交给解析器 → 收尾（3.7）。
7. 每个跟踪器的处理包在 `try/catch` 里：出错只记日志，不影响其他跟踪器。

**身份**（本文所有 `host.kill`、`host.isAlive` 调用都要带上）：`identityOfRun(运行) = { image: 运行.processImage, spawnedAtMs }`，放在 `engine/identity.ts`。`spawnedAtMs` 取 `spawnedAt` 的毫秒；升级前的旧记录没有 `spawnedAt` 时取 `startedAt + 60 秒`（进程号只在 60 秒的启动赛跑之内写进库，进程创建时间一定不晚于这个时刻）；两者都没有时为 null。引擎这边一律带身份，规则简单；进程托管自己知道哪些进程是它启动、句柄还在手里的（号不可能被复用），对这些不做核对，对接管来的进程才核对，号已属于别的进程就不动它（见 [进程托管](服务层-进程托管.md) 3.4、3.5）。例外：启动器里「刚拿到进程号就要结束」的两条路径（启动期间被取消、启动超时后迟到的成功）传 null。

### 3.7 收尾 `finisher`

```text
resolveRunOutcome({ progress, exit, killedBy: 库里运行的 killedBy, timeoutMs })
```

`exit`：有退出信息时 `{ kind: "exited", code, signal }`，接管后发现进程已不在时 `{ kind: "lost" }`。然后 `assertTransition(当前状态, 结局状态)`，一次性写入：状态、失败原因、说明、`endedAt`、`exitCode`、最终用量、`retry = null`、`activity`、`finalText`、`eventCount`。刷新时间线（出现 `run_end`）、发事件、唤醒等待者、请求放行。

### 3.8 超时 `timeouts`

每秒一次，对活跃运行调用 `findExpired(运行, now())`：

- `timeout`：进程号已知 → 写 `killedBy = "timeout"` 再 `host.kill(pid, 身份)`，由退出流程收尾；进程号还没拿到 → 这一轮跳过。
- `queue_timeout`：直接收尾为失败 `queue_timeout`，说明「排队超过 N 分钟」。

### 3.9 取消 `cancel(id)`

- 最新运行排队中 → 直接收尾为已取消，说明「已被取消」。
- 工作中 → 写 `killedBy = "cancel"`；进程号已知就 `host.kill(pid, 身份)`，还在启动就做标记（3.5 第 7 步）。之后最多等 5 秒让退出流程收尾，再返回最新摘要。
- 已是终态 → 原样返回。

### 3.10 时间线 `timelineStore`

- 一个苦工的完整时间线 = `assembleTimeline(每次运行 + 它的草稿)`，保证和从磁盘重新加载的结果一致。
- 草稿来源：在跑的运行用内存里的；已结束的读 `runs/<运行编号>/timeline.jsonl`；这个文件不存在（例如接管前就结束了）时，用新的解析器把 `out.jsonl`、`err.log` 从头解析一遍（时间用运行的 `startedAt`）。
- 缓存最近用过的 50 个苦工的完整时间线；在跑的苦工常驻缓存。
- 刷新（新草稿、运行开始、运行结束、新建运行时调用）：重新拼接，比原来多出来的尾部事件用 `timeline` 事件发出。时间线只会在尾部增长。
- `timeline(id, after, limit)`：返回 `seq > after` 的前 `limit` 条，`next` 为最后一条的 `seq`（没有就是 `after`），`total` 为总条数。

### 3.11 快照 `snapshotService`

- 查询口径照 [快照汇总](核心层-快照汇总.md) 3.1：活跃运行 + 结束时间晚于 `min(今日零点, now − snapshotWindowHours, now − 10 分钟)` 的运行 + 今日开跑的运行，涉及的苦工及其全部运行，全部项目。
- 排队位置：用 `queuePositions`（上限取配置，在跑和排队条目带上苦工的池和项目）。
- 今日零点按本机时区。
- 有任何变化时标记「脏」；`snapshot()` 在不脏且距上次计算不到 5 秒时返回缓存（健康窗口会随时间变化，所以最多 5 秒重算一次）。

### 3.12 等待 `waiter`

先检查编号是否都存在（不存在 `not_found`）；条件已满足立即返回；否则登记等待者，每次相关苦工的 `worker` 事件后重查；满足、超时或中止信号触发时返回 `WaitResult`：`done` 为已是终态的摘要（按请求顺序），`pending` 为其余编号，`timedOut` 为条件是否未满足。

### 3.13 其余接口方法

- `listWorkers`：`project` 参数先归一化成项目键；按创建时间倒序取 `limit` 个，算摘要，再按 `status` 过滤。
- `getWorker`：`buildWorkerDetail`。
- `pools()`：取快照里的池视图。`patchPool`：`config.save(applyPoolPatch(...))`，返回新快照里的该池视图。
- `roles()`：配置里的角色。`health()`：版本、启动时间、进程号、数据目录、端口。
- `requestShutdown()`：调用 `deps.onShutdownRequested`。
- 配置变化：`host.invalidate()`、快照标脏、发 `snapshot` 事件、请求放行。

### 3.14 服务启动时接管 `recovery`

对每个工作中的运行：

- 没有进程号（上次服务在启动它的途中退出）→ 失败 `interrupted`，说明「服务在启动苦工时退出」。
- `host.isAlive(pid, 身份)` 为真 → 接管：新建解析器，从头重新解析两份输出、重写 `timeline.jsonl`，之后按 2 秒一次探测存活（同样带身份）。进程号已被别的进程拿走时身份核对不通过，按下一条「进程已不在」处理。
- 存活探测要**先对全部工作中的运行并发发起**（进程托管会把同一轮的查询合成一次 PowerShell），拿到结果后再逐条接管或收尾；不要一条一条串行等，否则接管 20 个苦工要多等 30 多秒。
- 否则：从头解析两份输出后直接收尾，`exit = { kind: "lost" }`（输出里已经正常收尾的会被判为已完成）。

排队中的运行不用处理，正常参与放行。

### 3.15 过期清理 `retention`

启动时和之后每小时：`cutoff = now − retentionDays 天`；`listExpiredWorkerIds(cutoff)` 得到的苦工，删掉它们全部运行的 `runs/<运行编号>/` 目录，再删库里的记录；最后 `deleteOrphansCreatedBefore(cutoff)`。记一条日志写明删了多少。

### 3.16 出错隔离

所有定时任务、事件回调、跟踪器处理都包 `try/catch` 并记日志；任何单个苦工的问题不能让服务退出。

## 4. 装配规格

### 4.1 `resolveDaemonPaths(home)`

`config.json`、`daemon.json`、`fleet.db`、`daemon.log`、`runs/` 都在 `home` 下；单次运行的文件见 `app/types.ts` 注释。

### 4.2 `openConfigStore(file, logger)`

- 文件不存在：写入 `DEFAULT_CONFIG`（两空格缩进 JSON）再读。
- 首次读取不合法 → 抛 `FleetError("config_invalid", "配置文件有错：<问题列表>")`，服务不启动。
- 之后每秒检查一次文件修改时间：变了就重新解析；合法 → 替换当前配置、清空错误、通知；不合法 → 保留上一份有效配置、记下错误说明（问题列表用「；」连接）、通知。
- `save(config)`：写文件（两空格缩进），立即替换当前配置，并记下这次写入后的修改时间，避免被自己的轮询当成外部修改再通知一遍。

### 4.3 `startDaemon(options)`

```ts
interface StartDaemonOptions {
  home: string;
  /** 不传用配置里的端口；测试传 0 */
  port?: number;
  repoRoot: string;
  /** 看板构建产物目录，默认 <repoRoot>/apps/web/dist */
  webDistDir?: string;
  version: string;
}
interface DaemonHandle { port: number; token: string; home: string; stop(): Promise<void> }
```

1. 建好 `home` 和 `runs/`；创建日志。
2. 单实例：`daemon.json` 存在、进程号还活着、且 `/api/health` 能通 → 抛 `FleetError("conflict", "服务已在运行（端口 <端口>）")`。
3. 打开配置、数据库（`createRepos`）、进程托管（`createProcessHost`）。
4. 令牌：24 字节随机数的十六进制。
5. 创建引擎、HTTP 应用，按端口启动 HTTP（端口被占用 → 抛 `FleetError("conflict", "端口 <端口> 被占用")`）。
6. 写 `daemon.json`（进程号、实际端口、令牌、启动时间、版本、数据目录）。
7. `engine.start()`。
8. `stop()`：引擎停止 → HTTP 关闭 → 数据库关闭 → 配置关闭 → `daemon.json` 里的进程号还是自己时删掉它。可重复调用。

HTTP 关闭包含断开仍在推送的看板连接，见 [接口模块](服务层-接口.md#33-sse)。停止引擎仍保留独立运行的苦工；确认服务退出时要核对旧进程已经结束，不能仅凭端口不再监听。2026-09-26 查到两份旧服务卡在 SSE 连接上，合计占用约 2.5GB 内存；已复现并补充 `shutdownStreams.test.ts`，使完整关停能够走到数据库和配置清理。

本机复验：保持真实 SSE 连接后重启修复版，连接在重启时关闭，旧服务进程确认退出，新服务接管两条正在运行的任务，没有新增接管失败。清理时按进程号、创建时间、程序入口和是否监听核对身份；旧服务只结束自身，保留独立苦工。

### 4.4 `main.ts`

- 参数：`--home <目录>`、`--port <端口>`。`home` 缺省为环境变量 `FLEET_HOME`，再缺省为 `<用户目录>/.fleet-studio`。
- `repoRoot` = 本文件所在目录往上三级（`packages/daemon/dist/main.js` → 仓库根）；版本取 `packages/daemon/package.json`。
- 收到 `SIGINT` / `SIGTERM` 或退出请求：`stop()` 后以 0 退出。未捕获异常：记日志后以 1 退出。

## 5. 验证方式

- **引擎单测**（`packages/daemon/test/engine/`，引擎路负责）：内存数据库（`createRepos(":memory:")`）+ 假进程托管（实现 `ProcessHost`，可手动推送输出行和退出）+ 假配置存储。覆盖派活校验与建档、放行占位、启动失败、跟踪落库节流、收尾各分支、取消三种情况、续接、超时两种、`ended` 后强杀、等待三种返回、时间线增量与重新加载一致、快照缓存、接管三种情况、过期清理、池被删除；所有 `host.kill`、`host.isAlive` 调用都带上由运行记录拼出的身份（假进程托管记下收到的参数来断言），启动成功时写入 `spawnedAt`，旧记录（`spawnedAt` 为 null、`startedAt` 有值）身份里的时刻为 `startedAt + 60 秒`，两者都没有时为 null。
- **装配单测**（`packages/daemon/test/app/`，装配路负责）：路径布局；日志轮转；配置首次生成、非法、热更新（合法 / 不合法）、保存不触发重复通知；`daemon.json` 读写与只删自己的。
- **集成测试**（`packages/daemon/test/integration/`，测试路负责，在前两路完成后）：临时数据目录 + 端口 0 起真服务，运行时命令指向假苦工，逐条验证：
  1. 容量 3 的池同时派 9 个，任何时刻在跑不超过 3。两条互相独立的证据：假苦工轨迹（按每个进程独有的编号配对），以及测试期间每 100 毫秒问一次服务自己报的在跑数；失败时打印每个苦工的结局、轨迹全文和采样序列。
  2. 甲项目占满 3 个并排队 3 个后，乙项目派 2 个：下一个空位先给乙。
  3. 成功剧本：已完成、回报结论通过、用量累加、时间线含工具调用和结果。
  4. 模型错误、缺密钥、崩溃三种失败的原因与说明。
  5. 重试风暴：判为 `model_error` 且进程被结束。
  6. 运行超时：失败 `timeout`，进程（含子进程）被结束。
  7. 取消排队中和工作中的苦工。
  8. 续接：第 2 次运行复用会话（pi 的 `--session-id` 不变；opencode 带上首次运行的会话编号）。
  9. 重启接管：服务停掉时苦工继续跑，新服务起来后接着跟踪直到完成；服务停掉期间结束的苦工按输出判定结局。
  10. 调整容量后立即多放行。
  11. 快照里排队中的苦工带排队位置；全局 SSE 首条是快照。

## 6. 待扩展项

- 通道熔断：按池的最近失败率暂停放行。
- 取消后扫描残留 shell 子进程。

## 第二版（M5）：按优先级派活、统计与任务查询、原始输出清理

> 所属里程碑：[M5](../roadmap.md#m5) · 依据：[架构设计 2.2、2.3、4](../架构设计.md#4-容量调度规则)、[技术设计 8.4、8.5](../技术设计.md#85-按优先级派活)、[功能清单](../功能清单.md) 6.3、6.8、7.1～7.11、10.1～10.5 · 服务契约见 `packages/daemon/src/engine/service.ts`（主控已加方法）

### 1. 派活（`submit.ts`）

- 点名（`request.pool` 有值）：池不存在抛 `invalid_request`「池不存在：<id>」（沿用）；池停用抛 `FleetError("pool_disabled", "池 <id> 已停用，换一个池或不点名")`；池没配这个运行时沿用原报错。苦工记录 `requestedPool = poolId = <id>`，`model`、`channel`、`modelName` 取 `poolChannelModel(pool, runtime)`。
- 不点名：不再读 `config.defaults.pool`。至少要有一个池（不论启用与否）配了这个运行时，否则抛 `invalid_request`「没有配置 <runtime> 模型的池」。苦工记录 `requestedPool`、`poolId`、`model`、`channel`、`modelName` 全为 null。运行超时和排队超时：`resolveRunTimeoutMs` / `resolveQueueTimeoutMs` 需要池，没点名时用「单次覆盖 ?? 全局默认」（和池已删除时的降级一样），放行时**不再**按实际分到的池改超时。
- 其余不变：建完触发一次放行。所有池都停用时照样接受，任务留在公共排队（看板有提醒）。

### 2. 续接（`send`）

- 苦工 `poolId` 为 null（第一次运行还没被放行就结束了，比如排队时被取消）：新运行进公共排队。
- `poolId` 有值且池存在但停用：抛 `pool_disabled`「原来的池 <id> 已停用，续接要沿用原池」。
- 池已删除：沿用第一版（超时降级，下一轮放行判 `pool_removed`）。

### 3. 放行（`dispatcher.ts`）和快照输入（`snapshotService.ts`）

两处组装调度输入的重复代码合并成一个函数 `buildSchedulingInputs(config, activeRuns, workersById)`（新文件 `engine/schedulingInputs.ts`），返回 `{ limits, running, queued }`：

- `limits`：`config.pools` 按原顺序映射，带 `enabled` 和 `runtimes: poolRuntimes(pool)`。
- `running`：工作中的运行，`poolId` 取苦工的 `poolId`（为 null 的数据不一致条目跳过并记一条警告日志）。
- `queued`：排队中的运行，`requestedPoolId = worker.poolId`，`runtime = worker.runtime`。

放行拿到 `DispatchDecision[]` 后逐个处理：重读运行和苦工；苦工 `poolId` 为 null 时，在同一个事务里先把 `poolId`、`model`、`channel`、`modelName`（`poolChannelModel(分到的池, worker.runtime)`）写进苦工，再把运行改成工作中；然后照旧启动。`failPoolRemovedRuns` 只处理 `worker.poolId` 不为 null 且池已不存在的排队运行。

### 4. 收尾写 `runMs`（`finisher.ts`）

写终态时一并写 `runMs = endedAt − startedAt`（毫秒，`startedAt` 为 null 时为 null）。排队中被取消、排队超时的运行 `runMs` 为 null。

### 5. 快照（`snapshotService.ts`）

- 读结束运行的窗口从 `snapshotWindowHours` 改成 `max(snapshotWindowHours, 24)` 小时，保证池视图的 `recent` 有数据；苦工列表仍按 `snapshotWindowHours` 裁剪（核心层自己做）。
- `buildWorkerSummary` 多了 `nowMs` 参数，所有调用处（快照、`getWorker`、`listWorkers`、`wait`、任务查询）传当前时刻。

### 6. 原始输出清理（`retention.ts`）

- 不再删苦工、运行、项目的库记录，也不再删整个运行目录。
- 每小时一次：`cutoff = now − rawOutputRetentionDays 天`，分批（每批 200）取 `runs.listRawPurgeCandidates(cutoff, 200)`，对每个运行删 `out.jsonl`、`err.log`、`task.md`（不存在不算错），**保留** `timeline.jsonl`；删完 `markRawPurged`。单个运行删失败只记日志、不标记，下次再试。
- 确认时间线读取不依赖 `out.jsonl`：苦工详情和时间线只读 `timeline.jsonl`；只有服务重启接管在跑的苦工时才读 `out.jsonl`（在跑的不会被清理）。如果发现有别的地方在读已结束运行的 `out.jsonl`，改成读 `timeline.jsonl` 并在回报里说明。

### 7. 新增的服务方法（`queries.ts` 或新文件 `engine/statsQueries.ts`、`engine/taskQueries.ts`、`engine/poolSettings.ts`）

- `stats(query)`：
  1. `tz = { offsetMinutes: (ms) => -new Date(ms).getTimezoneOffset() }`（`engine/timeZone.ts`，测试里可以注入）。
  2. `resolveRange` 得到范围；`since = min(范围起点, 今天本地零点)`，all 时 since 为 null。
  3. `runFacts = repos.stats.runFacts(since)`；`workerFacts = repos.stats.workerFacts(since, runFacts 涉及的苦工编号)`。
  4. `labels`：项目名和项目色来自 `repos.projects.list()`；角色中文名来自配置（配置里没有的角色显示编号）；`seriesColor` 调 `repos.stats.seriesColor(kind, name, now)`。
  5. 调 `computeStats`。
- `tasks(query)`：范围用 `resolveRange`（按创建时间）；把请求翻成 `TaskQueryInput` 调 `repos.tasks.query`；对结果苦工批量取全部运行，用 `buildWorkerSummary` 生成摘要（`queuePositions` 用快照服务当前算好的那份）；返回 `{ items, nextCursor, total }`。
- `projects()`：`repos.projects.list()` 映射成 `ProjectInfo`，按名字排序（不区分大小写）。
- `setPoolEnabled(id, enabled)`：`config.updateRaw(raw => setPoolEnabledRaw(raw, id, enabled))`，之后和 `patchPool` 一样通知快照、触发放行，返回该池的视图。
- `reorderPools(poolIds)`：`config.updateRaw(raw => reorderPoolsRaw(raw, poolIds))`，之后同上，返回全部池视图。
- `pools()`：按配置顺序（即优先级）返回。

### 8. 验证

引擎单测（`packages/daemon/test/engine/`，用 `createTestEngine`）：

- 点名停用的池报 `pool_disabled`；点名启用的池苦工记录带上池、模型、渠道；不点名时这些为 null；没有任何池配这个运行时报错。
- 两个池（甲容量 1、乙容量 2，甲优先），派 4 个不点名的：甲 1、乙 2 立即放行并写进苦工记录，第 4 个在公共排队；甲的在跑结束后第 4 个进甲。
- 停用甲后，公共排队不再进甲；甲里在跑的不被打断；重新启用后恢复。
- 续接沿用原池；原池停用时续接报 `pool_disabled`；第一次运行在公共排队被取消后续接，新运行进公共排队。
- 收尾写入 `runMs`；排队取消的 `runMs` 为 null。
- 清理：到期运行的 `out.jsonl`、`err.log`、`task.md` 被删、`timeline.jsonl` 保留、库记录保留、标记为已清理；未到期的不动；删除失败不标记。
- `stats`、`tasks`、`projects` 的口径各一个用例（数值正确性已在核心层和存储层测过，这里只测接线）。
- `setPoolEnabled`、`reorderPools` 写回配置文件（读 `<home>/config.json` 验证）、生成备份、触发放行；排列不对报 `conflict`。

## 7. 改动历史

| 日期 | 改动 |
|---|---|
| 2026-09-23 | 规格定稿 |
| 2026-09-24 | 实现完成。引擎 96 个测试（假进程托管，但输出是真实临时文件、用真实的 tailer 和 pi / opencode 解析器）；装配 21 个测试；安装脚本用临时目录验证过安装、覆盖、冲突拦截、卸载。偏差：`EngineDeps` 多一个可选的定时器间隔（测试用，默认值照规格）；`DaemonHandle` 多一个 `onShutdownRequested`，让进程入口能收到接口发来的退出请求；接管时进程已不在也重写一份时间线文件；配置文件同样先写临时文件再改名 |
| 2026-09-24 | 独立评审修 6 条（收尾入口统一重读并核对预期状态；取消与超时谁先写谁算；启动阶段 60 秒超时；缓存条目随苦工清理删除；启动时接管或清理出错不阻止启动；启动失败清理端口与运行信息）。集成测试 22 个（起真服务 + 假苦工，覆盖第 5 节全部 11 条），揭示并修复：opencode 运行快于跟踪节拍时会话编号丢失 |
| 2026-09-24 | 集成测试加固（947 个测试全仓连跑 5 次全绿）：轨迹按进程独有编号配对；核对进程是否结束、收尾结束残留进程前都核对创建时间，防止进程号被复用后认错或误杀；容量测试加服务侧在跑数采样；各测试的准备与收尾步骤显式放宽时限。全仓负载下的一次容量测试失败累计 20 次全仓连跑未复现，最可能的解释是旧的按进程号配对把在跑数算虚高 |
| 2026-09-24 | 规格：结束和探测进程时带上身份（映像名 + 拿到进程号的时刻），启动成功时记下 `spawnedAt`，防止进程号被复用后误杀无关进程（3.5、3.6、3.8、3.9、3.14） |
| 2026-09-24 | 身份核对实现完成：启动成功时记下 `spawnedAt`，结束和探测进程都带身份（刚拿到进程号就要结束的两条路径传 null，句柄在手不必核对）；接管时的存活探测改为并发发起、合成一次查询。另按 3.6 第 6 步补齐：接管的苦工在探测里发现已退出时，先把输出读完、残行交给解析器再收尾（之前用的是探测前读到的内容，身份查询变慢后会把刚写完的回报漏掉、误判成中断）。引擎单测新增身份与接管并发的用例 |
| 2026-09-25 | 第二版（M5）规格定稿，待实现 |
| 2026-09-25 | 第二版实现完成：不点名派活按优先级、首次放行在同一事务写池和模型、停用报错、续接沿用原池、收尾写 runMs、只清理原始输出；五个新服务方法；主控用真实库副本起服务验证统计、任务、启停、排序 |
| 2026-09-26 | 修复看板实时连接导致服务关停等待不结束的问题；增加保持真实 SSE 连接时停止完整服务的回归验证 |
