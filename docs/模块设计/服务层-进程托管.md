# 服务层 · 进程托管

> 模块定位：找到运行时可执行文件、补齐环境变量、分离启动苦工、增量读输出、判断存活、结束进程树 · 对应代码：`packages/daemon/src/process/` · 所属里程碑：[M1 闸门服务与 fleet 命令](../roadmap.md#m1) · 状态：已完成 · 最近更新：2026-09-26

## 1. 职责与边界

- **管：** 和操作系统打交道的全部动作。
- **不管：** 启动参数怎么拼（运行时适配器）、什么时候启动和结束（调度引擎）、输出是什么意思（解析器）。
- 事实依据：[pi 运行时调研](../调研/pi-运行时.md) 第 1、2、6、7 节；[opencode 运行时调研](../调研/opencode-运行时.md) 第 1、5、6 节。

## 2. 结构

| 文件 | 导出 |
|---|---|
| `process/types.ts` | 契约：`ResolvedCommand`、`SpawnRequest`、`SpawnedProcess`、`ProcessExitInfo`、`ProcessHost`、`OutputTailer`（主控已写好） |
| `process/backgroundCommand.ts` | `runBackgroundCommand`：后台辅助命令统一隐藏控制台，保留标准输出、报错输出及失败信息 |
| `process/windowsWorkerEnv.ts` | 仅给 Windows Node 苦工追加预载策略，保留已有 `NODE_OPTIONS`，其他运行时和平台沿用原环境 |
| `process/windowsChildPolicy.ts` | 苦工内预载的子进程策略，覆盖运行时及扩展的同步、异步、Promise 和派生 Node 命令 |
| `process/runtimeResolver.ts` | `createRuntimeResolver` |
| `process/userEnv.ts` | `readRegistryEnvironment`、`parseRegQueryOutput`、`expandEnvReferences`、`mergeWorkerEnv`、`dropInheritedProxyVars` |
| `process/spawnWorker.ts` | `spawnWorker` |
| `process/killTree.ts` | `killTree` |
| `process/processProbe.ts` | `isProcessAlive`、`parseTasklistCsv` |
| `process/identityProbe.ts` | `IDENTITY_CLOCK_TOLERANCE_MS`、`parseProcessFactsLines`、`matchesIdentity`、`createIdentityProbe`（3.5） |
| `process/outputTailer.ts` | `createOutputTailer` |
| `process/processHost.ts` | `createProcessHost(options): ProcessHost`，把以上组装起来；内含自己启动的进程号名单、完整核对结果缓存与 `IDENTITY_RECHECK_MS`（3.5 性能约束）；可注入身份查询函数和时钟（测试用） |
| `process/index.ts` | 汇总导出 |

## 3. 规格

### 3.0 后台辅助命令

所有后台辅助命令都通过 `runBackgroundCommand(executable, args)` 执行。统一使用 `execFile` 的 `windowsHide: true`，输出按 UTF-8 返回，失败仍向调用方抛出；参数按数组原样传递，不启用 shell。运行时定位中的 `cmd.exe /d /s /c "npm root -g"` 仍是固定命令的唯一例外。

| 命令 | 调用时机 |
|---|---|
| `tasklist` | 检查进程是否存在、映像名是否匹配；接管的苦工每 2 秒检查一次 |
| PowerShell `Get-CimInstance` | 核对进程创建时间；接管时检查，存活轮询的完整核对缓存 30 秒，结束接管的进程前重新检查 |
| `reg query` | 补齐苦工环境变量，结果缓存 60 秒 |
| `cmd.exe` / `npm root -g` | 默认 npm 全局目录不存在时查运行时安装位置 |
| `taskkill` | 取消、运行超时或已结束但进程迟迟不退出时结束进程树 |

此前只有常驻服务和苦工自身的启动设置了隐藏窗口，辅助命令遗漏了该设置。从没有终端的后台服务启动这些命令时，Windows 可能创建可见控制台；接管轮询会反复触发。统一入口消除这条来源，不改变存活判断、身份保护、取消和超时规则。

**苦工内部命令也要覆盖。** pi 0.87.0 的普通 shell 工具已有隐藏窗口设置，但文件查找、文本搜索、扩展执行入口仍有遗漏；pi-web-access 0.30.0 也直接调用外部命令。单独隐藏 Fleet 拉起苦工的那一层无法约束这些调用。

- Windows 上以 `node.exe` 启动苦工时，通过子进程环境里的 `NODE_OPTIONS --import` 在运行时加载前预载窗口策略；保留已有预载和参数，按不区分大小写的环境变量名合并，文件地址支持空格，不修改调用方环境对象。
- 策略覆盖 `spawn`、`spawnSync`、`exec`、`execSync`、`execFile`、`execFileSync`、`fork`。只补充隐藏窗口选项，命令、参数、环境、输出、超时、退出码和取消信号仍交给 Node 原实现。
- 同步更新 ESM 具名导入；CommonJS 和 Node 自定义的 Promise 执行入口同样生效，并保留 Promise 上的子进程句柄。后续 Node 子进程正常继承该环境，继续受同一策略约束。
- 策略随仓库构建，无需修改全局 pi 或插件的安装文件。新启动和续接的苦工生效；已经运行的进程保留原加载内容。原生非 Node 运行时、主动清掉预载环境的后代，以及程序主动创建的图形窗口不在此策略覆盖范围。

### 3.1 找可执行文件 `createRuntimeResolver({ getConfig, npmGlobalRoot? })`

优先用配置 `runtimes.<运行时>.command`：`executable = command[0]`，`prefixArgs = command.slice(1)`，`image = executable 的小写文件名`。没配时自动探测，结果缓存到 `invalidate()` 为止：

1. **npm 全局目录**：先试 `%APPDATA%\npm\node_modules`（Windows）；不存在再执行一次 `npm root -g`（Windows 下 npm 是 `.cmd`，用 `cmd.exe /d /s /c "npm root -g"` 执行，这条命令不含任何外部输入）。可注入 `npmGlobalRoot` 方便测试。
2. **pi**：读 `<全局目录>/@earendil-works/pi-coding-agent/package.json` 的 `bin.pi`（可能是字符串或对象），拼出入口脚本。`executable = process.execPath`，`prefixArgs = [入口脚本]`，`image = "node.exe"`（非 Windows 取 `node`）。
3. **opencode**：读 `<全局目录>/opencode-ai/package.json` 的 `bin.opencode`，拼出可执行文件。`prefixArgs = []`，`image = 该文件的小写文件名`。
4. 入口文件不存在 → 抛 `FleetError("runtime_unavailable", "找不到 <运行时>：<尝试过的路径>")`。

**绝不 spawn `.cmd`、`.ps1` 外壳**，调研实测它们会把多行任务截断在第一个换行处。

### 3.2 环境变量

- `readRegistryEnvironment()`：仅 Windows。执行 `reg query HKCU\Environment` 和 `reg query "HKLM\SYSTEM\CurrentControlSet\Control\Session Manager\Environment"`，用 `parseRegQueryOutput` 解析 `REG_SZ` / `REG_EXPAND_SZ` 行，`REG_EXPAND_SZ` 的值用 `expandEnvReferences` 展开 `%NAME%`。用户级覆盖系统级。非 Windows 返回空对象。读失败返回空对象并记日志，不抛异常。
- `mergeWorkerEnv(base, registry)`：以当前进程环境为底，注册表里有、当前环境没有的变量补进去；Windows 下变量名比较不区分大小写；`Path` / `PATH` 不从注册表补（当前进程的 PATH 已经包含它们，重复拼接会越来越长）。
- `dropInheritedProxyVars(env)`：当前进程环境这一侧先去掉 `HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY`、`NO_PROXY`（不区分大小写），再交给 `mergeWorkerEnv`。服务进程的这几个变量来自拉起服务的那个会话（例如 codeg 里的会话带着自己的本地代理），随拉起者变化；苦工的代理只由 pi 启动命令读的环境文件决定，而那份文件盖不过已经存在的同名变量。注册表里用户自己设的全局代理照常补进去。
- **任何日志、错误信息里都不准出现变量的值**，最多写变量名。
- `workerEnv()` 缓存结果 60 秒，之后下次调用重新读注册表（用户新设了密钥不用重启服务）。

### 3.3 分离启动 `spawnWorker(request)`

1. 检查 `cwd` 存在且是目录，否则抛 `FleetError("invalid_request", "工作目录不存在：<cwd>")`。
2. 为 `files` 里每个文件建好父目录并写入（UTF-8）。
3. 为 stdout / stderr 文件建好父目录，以追加方式打开得到文件描述符。
4. 按 3.0 补齐 Windows Node 苦工的预载环境，再 `spawn(executable, [...prefixArgs, ...args], { cwd, env, detached: true, windowsHide: true, stdio: ["ignore", outFd, errFd] })`。**stdin 必须是 `"ignore"`**：两个运行时在 stdin 不关闭时都会永久卡住。不用 `shell`。
5. 立刻 `child.unref()`，并在父进程里关闭两个文件描述符（子进程已经继承）。
6. 监听 `error`：启动阶段出错（例如可执行文件不存在）→ 抛 `FleetError("runtime_unavailable", "启动失败：<原因>")`。拿到 `pid` 才算启动成功。
7. `onExit` 包装 `child.on("exit")`，把 `code`、`signal` 交给监听者；监听者在进程已退出后才注册时也要能收到（缓存退出信息）。

分离启动的用意：服务重启、崩溃都不会连带杀死苦工，重启后可以接管。

### 3.4 结束进程树 `killTree(pid)`

- Windows：`taskkill /PID <pid> /T /F`（隐藏窗口，不经过 shell）。失败后重新查询进程是否存在；已经退出视为成功，仍存在则抛错，不依赖报错文字或控制台编码。
- 其他平台：先 `process.kill(-pid, "SIGKILL")`（分离启动的进程是进程组组长），失败再 `process.kill(pid, "SIGKILL")`，`ESRCH` 视为成功。
- `ProcessHost.kill(pid, identity)`：`identity` 非 null 时**先按 3.5 核对身份**，不是同一个进程（或进程已不在）就静默返回，绝不对它调用 `killTree`；核对通过才 `killTree(pid)`。`identity` 为 null 时直接 `killTree(pid)`。

### 3.5 存活判断与身份核对

**为什么要核对身份：** Windows 会很快把进程号重新分给新进程（2026-09-24 实测：连续起 300 个短命 node 进程，约 25 次拿到用过的号，最早隔 67 次启动就复用）。服务自己启动、还握着句柄的进程不会被复用号；但服务重启后接管的进程手里没有句柄，只凭进程号认人。苦工的映像名多半是 `node.exe`，本机随时有别的 node 进程在起，只核对映像名挡不住。认错的后果是把一个无关进程连同它的子进程树一起结束，所以结束和判断存活之前都要核对「这个号现在还是不是当初那个进程」。

**身份** `ProcessIdentity = { image: string | null; spawnedAtMs: number | null }`（契约在 `process/types.ts`）：`image` 是启动时记录的映像名；`spawnedAtMs` 是拿到进程号的时刻，真正的那个进程创建时间一定不晚于它。旧记录没有这个时刻时为 null，只核对映像名。

**`isProcessAlive(pid, image)`**（保留，快速路径）：

- Windows：`tasklist /FI "PID eq <pid>" /FO CSV /NH`（`execFile`），用 `parseTasklistCsv` 解析出映像名；没有这一行 → 不活；`image` 非 null 时映像名不一致（不区分大小写）→ 视为不活。
- 其他平台：`process.kill(pid, 0)` 不抛异常即活；不核对映像名。

**`identityProbe.ts`：**

- `IDENTITY_CLOCK_TOLERANCE_MS = 2000`。
- `parseProcessFactsLines(output): Map<number, { image: string; createdAtMs: number }>`：解析下面查询命令的输出，每行 `<进程号>|<映像名>|<创建时间毫秒>`；空行、字段不全、数字解析失败的行跳过。
- `matchesIdentity(facts, identity): boolean`：`identity.image` 非 null 时映像名不区分大小写相等；`identity.spawnedAtMs` 非 null 时 `facts.createdAtMs ≤ spawnedAtMs + IDENTITY_CLOCK_TOLERANCE_MS`；两项都满足（为 null 的项不核对）才为真。
- `createIdentityProbe(query?)`：返回 `lookup(pid): Promise<{ image; createdAtMs } | null>`。同一轮事件循环里的多个 `lookup` **合并成一次查询**（用 `setImmediate` 攒一批），避免接管了很多苦工时每 2 秒起一堆 PowerShell。默认 `query` 在 Windows 上执行（`execFile`，不经过 shell）：
  ```text
  powershell -NoProfile -NonInteractive -Command
    Get-CimInstance Win32_Process -Filter "ProcessId=<号1> OR ProcessId=<号2> ..." |
    ForEach-Object { "{0}|{1}|{2}" -f $_.ProcessId, $_.Name, ([DateTimeOffset]$_.CreationDate).ToUnixTimeMilliseconds() }
  ```
  进程号只来自数据库里的整数，拼进命令前必须确认是正整数。可注入 `query` 方便测试。

**`ProcessHost.isAlive(pid, identity)`：**

- `identity` 为 null → 只看这个号有没有进程（`isProcessAlive(pid, null)`）。
- `identity.spawnedAtMs` 为 null → `isProcessAlive(pid, identity.image)`（旧记录，与改动前一致）。
- 否则（仅 Windows）→ `lookup(pid)`；查不到 → 不活；`matchesIdentity` 为假 → 不活（号已属于别的进程）。
- 查询本身出错（PowerShell 起不来、WMI 故障）→ 记一条警告日志，退回 `isProcessAlive(pid, identity.image)`。
- 其他平台忽略 `spawnedAtMs`，只核对存在（Linux 进程号顺序分配，复用慢得多）。

**性能约束（2026-09-24 实测：一次 PowerShell 身份查询约 1.7 秒，`tasklist` 约 0.09 秒）：**

1. **自己启动的进程走快路径。** 进程托管记下自己 spawn 出来、还没收到退出事件的进程号（spawn 成功时加入，退出事件触发时移除）。这些进程句柄还在手里，号不可能被复用：`kill` 对它们不核对身份，直接 `killTree`；`isAlive` 对它们只用 `isProcessAlive(pid, null)`。这样取消、超时结束自己启动的苦工不会多等 1.7 秒。
2. **接管的进程降频做完整核对。** 对不是自己启动的进程号，`isAlive` 每次先用 `isProcessAlive(pid, identity.image)` 快速核对存在和映像名，不通过直接返回 false（退出能在 2 秒内发现）；通过时，同一个「进程号 + spawnedAtMs」的完整身份核对（`lookup`）最多每 30 秒做一次，核对通过的结果缓存 30 秒（常量 `IDENTITY_RECHECK_MS = 30_000`）。号在苦工退出后 2 秒内就被同名进程复用的情况，最迟 30 秒内被发现。进程被判不活时删掉它的缓存。
3. **`kill` 对不是自己启动的进程每次都做完整核对，不看缓存。** 结束进程不可逆，宁可慢 2 秒。

### 3.6 增量读输出 `createOutputTailer(filePath, startOffset = 0)`

- `readNew()`：文件不存在返回空数组；否则从当前偏移读到文件末尾，用 `StringDecoder("utf8")` 解码（多字节字符跨读取边界时不能乱码），与上次残留拼接后按 `\n` 切行，每行去掉末尾 `\r`，最后一段不完整的留到下次。单次最多读 4MB，剩下的下次再读。
- `flush()`：返回残留的最后半行（去掉 `\r`），没有返回 null；调用后残留清空。
- 文件被截短（大小小于偏移）时从头重新读。

### 3.7 组装 `createProcessHost({ getConfig, logger, identityQuery?, now? })`

把以上各项装进 `ProcessHost` 接口，`invalidate()` 同时清掉可执行文件缓存和环境变量缓存。

## 4. 验证方式

`packages/daemon/test/process/` 下：

- 后台命令：真实命令验证中文、换行、引号和 shell 特殊字符原样传递，标准输出与报错输出可读，非零退出码仍可识别；Windows 下先按常驻服务相同方式分离启动一个后台 Node 进程，再由它调用 PowerShell，通过系统接口读取控制台可见性，确认没有可见控制台。测试位于 `backgroundCommand.test.ts`。
- 苦工内部命令：`windowsWorker.test.ts` 从真实分离的 Node 苦工调用同步、异步、回调及 Promise 执行入口，逐个检查控制台不可见；验证原有预载保留、ESM 导入一致、Promise 子进程句柄可用，以及派生 Node 进程继承策略。
- 解析函数纯测：`parseRegQueryOutput`（含 `REG_EXPAND_SZ`、值里带空格、空值）、`expandEnvReferences`（存在与不存在的变量）、`mergeWorkerEnv`（大小写、PATH 不补）、`dropInheritedProxyVars`（四个代理变量不区分大小写地去掉、名字只是带 PROXY 字样的保留、不改入参、和注册表合并时用户设的全局代理照常补进去）、`parseTasklistCsv`（有进程、无进程的「信息：没有运行的任务」提示）。
- 可执行文件探测：用临时目录伪造一个 npm 全局目录（`package.json` 的 `bin` 分别是字符串和对象两种写法）；配置里写死 `command` 时优先用配置；找不到时报中文错误。
- 分离启动（真实进程）：用 `process.execPath` 跑一个临时脚本，验证多行、带引号、带中文的参数原样到达；stdin 是关闭的（脚本读 stdin 立刻得到结束）；stdout / stderr 分别写进文件；退出码能拿到；工作目录不存在时报错。
- 结束进程树：启动一个会再拉起子进程、然后睡眠的脚本，`killTree` 之后父子进程都不在了（用 `isProcessAlive` 确认）。
- 身份核对：`parseProcessFactsLines`、`matchesIdentity` 纯测（映像名大小写、创建时间恰好在容差边界上、为 null 的项不核对）；`createIdentityProbe` 注入假查询，确认同一轮里 3 个 `lookup` 只查询 1 次、各自拿到自己的结果；真实进程：起一个睡眠的 node 进程，用正确的身份 `isAlive` 为真；把 `spawnedAtMs` 设成比它的真实创建时间早 10 秒（等价于「这个号现在属于一个更晚创建的进程」）时 `isAlive` 为假、`kill` 不动它（之后它仍然活着，测试收尾再用正确身份结束它）；快路径：自己 spawn 的进程 `kill`、`isAlive` 不触发身份查询（注入会计数的假查询断言调用次数为 0），退出后移出名单；降频：同一进程号 30 秒内连续 `isAlive` 只触发 1 次身份查询，超过 30 秒再触发 1 次，`kill` 每次都触发。
- 增量读：分几次追加内容（含 CRLF、跨读取边界的中文、末尾半行），逐次读出的行正确；`flush` 取到最后半行；文件被截短后从头读。

2026-09-26 验证：`pnpm check` 通过（163 个测试文件、1323 个测试），含后台命令的 3 个真实进程测试。构建后重启本机常驻服务，重启前正在执行的 5 个苦工全部保持运行，最近活动时间全部继续推进，没有新增 `interrupted` 失败。

同日继续验证苦工内部命令：完整门禁 165 个测试文件、1325 个测试通过。独立调用已安装 pi 的普通 PowerShell、嵌套 cmd、bash 内嵌套 cmd 和扩展执行入口；扩展入口修复前系统报告控制台可见，加载策略后四条路径都不可见且命令成功。清理两份旧服务、六个卡住的测试进程和两个假苦工残留，逐一核对后确认均已退出；其余 Codeg/Codex 会话、浏览器助手及已有网页开发服务按当前用途保留。

## 5. 待扩展项

- 取消后扫一遍残留的 shell 子进程（opencode 调研建议的兜底），等实测出现残留再做。

## 6. 改动历史

| 日期 | 改动 |
|---|---|
| 2026-09-23 | 契约与规格定稿 |
| 2026-09-23 | 实现完成（56 个测试，全部用真实进程）。实测确认多行任务原样送达、标准输入已关闭、结束进程树时子进程一并结束。结束进程树改为：taskkill 失败后复查进程是否还在，不按报错文字判断（实测报错文字会因控制台编码乱码，且「找不到进程」和「拒绝访问」退出码相同） |
| 2026-09-24 | 规格：结束与存活判断加身份核对（映像名 + 创建时间不晚于拿到进程号的时刻），防止进程号被复用后认错进程、误杀无关进程树（3.4、3.5） |
| 2026-09-24 | 身份核对实现完成：合批查询、自己启动的进程走快路径、接管的进程完整核对每 30 秒一次、结束前每次完整核对。实测取消自己启动的苦工 0.19 秒返回；主会话验收场景（进程号填成一个无关 node 进程的号再重启服务）修复前该进程 1.9 秒后被误杀，修复后安然无恙，模拟升级前的旧记录同样不误杀 |
| 2026-09-25 | 苦工环境去掉从拉起服务的会话继承来的代理变量（`dropInheritedProxyVars`）：苦工改由 pi 启动命令读环境文件走本机代理后，继承来的代理会盖掉文件里的设置，而服务是谁拉起的就继承谁的环境 |
| 2026-09-26 | 后台辅助命令统一隐藏窗口，覆盖存活检查、身份核对、环境读取、运行时探测和结束进程；新增从分离后台进程调用 PowerShell 的控制台可见性验证 |
| 2026-09-26 | 继续覆盖苦工内部的检索及扩展命令：Windows Node 苦工预载窗口策略；实际 pi 扩展命令测试从“控制台可见”变为“不可见”，普通命令和嵌套 cmd 仍正常执行 |
