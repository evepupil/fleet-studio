# 2026-09-25 进度

> 项目：fleet-studio · 当日主题：谈容量条（每个模型池一行）的改版，暂未开工，不属于已有里程碑
> 派活方式：经 fleet，dsf 池

## 容量条改版调研（00:00–00:40）

1. **这一轮几路：** 1 路侦察（wy4s9d），查五家产品怎么在一屏里展示多个供应商或资源池的忙闲、健康和速度。
2. **回来了：** 第 1 次运行一直在干活（下载截图、文字识别、绕开被污染的域名解析），干满 30 分钟被超时结束，回报一个字没交，用了约 790 万 tokens，九成是缓存。续接一句「别再查了，按已看到的写回报」，2 分钟交齐五家的结论。
3. **验收结论：** 通过。每家都标明了哪些是亲眼看到、哪些没看到；OpenRouter 和 Nomad 没拿到截图，已在报告开头注明。原文存进 [供应商与资源池展示](../调研/供应商与资源池展示.md)。
4. **介入次数：** 0（续接只是催交，没改业务代码）。
5. **本家子代理：** 没用。
6. **下一步：** 把调研补出的几条给用户，连同上一批清单一起删改；删完出版式示意图。

教训：一路查五家、每家还要下载截图做识别，30 分钟不够，便宜模型也不会自己掐着时间收尾。下次这类调研按产品拆成几路并行，或者任务书里直接写「超过 20 分钟就按已看到的交回报」。这次也暴露出超时直接判失败会把已经查到的东西全丢掉，已作为「超时自动催交」补进改版的讨论清单。
# 2026-09-25 进度

## 第二版看板：功能清单调研

1. 在跑：6 路侦察，各查一个同类产品的看板功能——New API（wjgpn5）、LiteLLM（w9yun4）、OpenRouter（wcaa26）、Helicone（wqavvz）、Cloudflare AI Gateway（wcf58f）、Bull Board 与 Sidekiq（wysfp8）。
2. 回来的：6 路全部完成（4～15 分钟），原文存进 [调研/看板同类产品](../调研/看板同类产品.md)。
3. 验收结论：每条带出处，可用；由一路 sonnet 子代理对照已定需求筛出候选功能（报告合计 15 万字，交给它筛是为了不把原文搬进主会话）。
4. 介入次数：0。
5. 本家子代理：1 路 sonnet，筛调研报告；没走 fleet 是因为只读一个本地文件、需要对照对话里的需求判断取舍。
6. 下一步：先给用户列功能清单大类，调研回来再补进清单。

## M5 第二版底座：开工

> 里程碑：[M5](../roadmap.md#m5)

主控先把接口契约写成代码（配置结构、请求校验、响应形状、调度类型、统计输入类型、服务方法、配置写回接口），再写了 9 份模块规格（新增 [统计口径](../模块设计/核心层-统计口径.md)，其余 8 份各加「第二版」一节）。侦察 6 路（whb2g7、w922ec、wecrwa、wh3w77、wvvdd2、wbjn4s）摸清了引擎、存储、接口、命令行与 skill、前端、测试设施的现状。

调度表（第一波）：

| 路 | 角色 | 在干什么 | 苦工 | 状态 |
|---|---|---|---|---|
| 1 | 实现 | 配置：启停和排序写回、保留期改名、备份 | wqbj3w | 跑着 |
| 2 | 实现 | 调度：按优先级放行、公共排队 | wxz2cn | 跑着 |
| 3 | 实现 | 统计口径纯函数 | wh9fse | 跑着 |
| 4 | 实现 | 快照：新字段、实时卡片的数 | wtzpfe | 跑着 |
| 5 | 实现（luna） | 存储：数据库版本 3、统计和任务查询 | wwygfa | 跑着 |
| 6 | 实现 | 命令行显示 | wmrb5y | 跑着 |
| 7 | 实现 | skill 去掉默认池 | wmnk5j | 跑着 |

- 介入次数：0（契约代码是主控职责内的活，不算介入）。
- 本家子代理：未用。
- 已知：全仓测试里 retryStorm 集成测试侦察时失败一次，属于记录过的偶发进程测试，收尾时单独重跑确认。
- 下一步：等第一波回来验收；同时写 M6 的站点层规格；第二波是调度引擎接线、接口路由，以及让第一版看板先跟上新类型（保证门禁能过）。

### 检查点（13:15 前后）

| 路 | 角色 | 在干什么 | 苦工 | 状态 |
|---|---|---|---|---|
| 1 | 实现 | 配置写回与备份 | wqbj3w | 验收通过 |
| 2 | 实现 | 调度：按优先级放行 | wxz2cn | 验收通过（45 个测试） |
| 3 | 实现 | 统计口径 | wh9fse | 验收通过（96 个测试），返工一次：拆超长文件 |
| 4 | 实现 | 快照新字段 | wtzpfe | 验收通过 |
| 5 | 实现（luna） | 存储版本 3 | wwygfa | 跑满 30 分钟超时，大部分已写完；续接并补渠道、模型两个筛选 |
| 6 | 实现 | 命令行显示 | wmrb5y | 验收通过 |
| 7 | 实现 | skill 去掉默认池 | wmnk5j | 验收通过，补改一句保留期 |
| 8 | 实现 | 接口路由与看板令牌 | w8uxv3 | 验收通过（93 个测试） |
| 9 | 实现（luna） | M6 地基：shadcn/ui + Tailwind | wy2ckr | 跑着 |

- 主控复跑：core、cli、配置写回测试共 715 个全过；core 类型检查零错误。
- 契约补了一处：任务列表加「渠道」「模型」两个筛选（总览按模型、按渠道的排名点进任务页要用）。
- M6 规格已写完：站点层 `apps/web/DESIGN.md`，页面层 `apps/web/design/` 下骨架、总览、槽位、任务、演示数据五份。
- 介入次数：0。
- 下一步：存储完成后派调度引擎接线；地基完成后核对四样，再派演示数据、总览、槽位、任务表格、任务详情五路。

### 检查点（14:30 前后）

- M5 后端全部落地：调度引擎接线（w6vcna，luna）验收通过；主控复跑 packages 下 146 个测试文件、1190 个测试全过，四个包类型检查零错误。
- 主控端到端：把本机真实库复制到临时目录（先把副本里在跑、排队的运行标成失败、池容量改成 0，避免测试服务碰到真实服务的苦工进程），起一个 4891 端口的服务。结果：升级前自动备份、库从版本 2 升到 3；统计接口在 97 个真实任务上给出合理的数（3.6 亿 token、四个模型、按小时分段对齐本地零点）；任务列表翻页正常；看板令牌能启停、能排序，调改容量接口被拒（401），传错排列 409，每次改动留一份配置备份，令牌不进 daemon.json；命令行 `fleet pools` 按优先级列出并显示停用，不点名派活显示「公共排队第 1 位」。
- 评审：核心层（wbf99a，luna 评 dsf 写的）4 条、存储层（w7uxk4，glmf 评 luna 写的）6 条，逐条判断都成立；其中 1 条（按合计值排序翻页时数值变化）属游标翻页固有限制，记进存储模块文档不改；其余两路修复（w2fzpa、wpu6ja）已验收。调度引擎与接口层的评审（w6gszx，glmf）超时后续接中。GLM 池做评审两次都跑满 30 分钟，下次评审换别的池或给更长时限。
- M6 地基（wy2ckr，luna）跑满 90 分钟被结束，主控复跑确认基本完成（单测、构建、颜色检查通过），续接补三处后加的规格；同时已派槽位（wyrrkd）、任务详情（w7yh9c）、演示数据（w6natk）三路。
- 介入次数：1（令牌文件 tokens.css 的格式化，主控独占文件）。

### 收尾（M5 完成，M6 待用户确认）

- 第二批路：总览（wrjwdh，luna）、槽位（wyrrkd）、任务详情（w7yh9c）、演示数据（w6natk）、任务表格（第一次 wwxmde 派给 dsf，40 分钟一个文件没写，取消后换 luna 重派 wp9yrg，18 分钟完成）、验收脚本（wcf4gn）、界面修复（wpvxw7，8 处）、集成测试（wm9q6k）。dsf 上三路都跑满 60 分钟被结束，主控复核它们都已在最后检查阶段，验收通过。
- 主控介入 3 次：令牌文件格式化；AppShell 补绑任务详情的仓库（一行，详情一直停在加载中的根因）；fleet pools 兼容还没重启的第一版服务（新命令对旧服务会把所有池显示成「停用」）。另改了验收脚本里两条写错的期望值（规格笔误，不是页面问题）。
- 评审：调度引擎与接口层（w6gszx，glmf）两条指向配置写回的问题是评审读到了修复进行中的文件，核对当前代码已正确；时间线在 timeline.jsonl 缺失时回退读原始输出属于正常兜底，不改。
- 结果：门禁 162 个测试文件、1320 个测试全过；截图 80 张无溢出；交互检查 52/52；真实库副本上起新服务，三页正常，在页面上下移成功写回配置并留备份。
- 已知小问题（留给用户过目）：槽位表格在 800 宽时「渠道 · 模型」列被挤到只剩两个字母。
- 本机常驻服务仍是第一版（pid 6424）。重启后第二版生效：先把库备份到 db-backups/ 再升到版本 3。没替用户重启，这属于动线上服务。
