# 2026-09-23 开发进度

> 项目：fleet-studio · 当日里程碑：设计定稿、[M0 工程骨架与领域核心](../roadmap.md#m0)
> 派活方式：全部派给主会话的原生子代理，统一用 sonnet（用户要求，用来和 wiki-forge 那次用 dsf 苦工的效果对比）。

## 设计定稿

- 需求、技术、架构三份设计和 roadmap 由主会话直接写完。用户授权主会话自行决定需求细节，主会话拍的板：闸门服务自己拉起苦工进程；容量按「谁占得少谁先补」分配；状态在用户说的三种之外加「排队中」「已取消」；排队默认不限时；新增「测试」角色。
- 后续各路并行开发共用的接口契约（状态、记录、时间线、配置结构、接口数据形状）由主会话亲手写成代码，不外包，理由是契约错一处几路一起返工。

## 第一轮：三路调研（并行）

| 路 | 干什么 | 结果 |
|---|---|---|
| 1 | pi 的启动方式和事件流 | 通过。中途遇网关 502 中断一次，续接后完成 |
| 2 | opencode 的启动方式和事件流 | 通过。中途遇网关 502 中断一次，续接后完成 |
| 3 | 同类产品界面调研 | 通过，看到 10 个产品的真实界面，2 个看不到的如实剔除 |

- **调研挖出的关键事实**（直接改写了设计）：pi 遇到通道错误会无限重试、永不自己退出；两个运行时都必须关掉标准输入，否则永久卡住；经过 cmd 外壳的多行任务会在第一个换行处被截断（这意味着现在 pi-fleet 用 PowerShell 派活，多行任务可能一直被截断）；json 模式下 pi 失败也返回退出码 0。
- **意外一：另一个会话的跑偏子代理闯进本仓库。** 它把本目录当成项目搭骨架，改写了核心包的两个配置文件，还往调研样本目录写了同名抓包。主会话发现后联系了那个会话，对方确认并已停止它；两路调研逐个核对样本归属，把可疑的删掉重抓。
- **意外二：安全事件。** opencode 调研路过滤配置文件时，把 CloudMind MCP 的访问令牌打印进了自己的一次工具输出。令牌没有写入任何文件，但已经进了那段对话记录，需要用户决定要不要轮换。
- 主会话介入次数：0

## 第二轮：工程骨架（单路）

- **做了什么**：pnpm 工作区、最严格的 TypeScript、Biome、Vitest、门禁脚本、四个包和前端的空壳。
- **验收结论**：通过，一次成功，门禁全绿，契约代码只被格式化工具改了换行。
- 主会话介入次数：0

## 第三轮：核心层三路（并行）

| 路 | 干什么 | 结果 |
|---|---|---|
| 配置 | 编号、默认配置、配置解析与查询、项目路径归一化、配色 | 通过，93 个测试 |
| 调度 | 公平放行、排队位置、超时、状态流转、结局判定 | 通过，71 个测试 |
| 快照 | 看板快照、苦工摘要、回报解析、时间线拼接 | 通过，57 个测试 |

- 第一次派出去三路全部撞上网关 502，一个文件都没写，原样重派。
- 快照路写到最后一个测试文件时撞上 sonnet 用量上限（提示晚上 7 点重置），额度恢复后续接收尾。**因为这次额度中断，流水线空转了约一个半小时。**
- 快照路自己挖出并修掉一个真问题：回报里写成 `**SUMMARY**: 文字`（加粗只包住段名）时解析不出来。
- 主会话逐行复核了调度和结局判定两份核心代码，规则与规格一致。
- 主会话介入次数：0

## 第四轮：运行时适配两路（并行）

| 路 | 干什么 | 结果 |
|---|---|---|
| pi | 启动参数、事件流解析、结局判定 | 通过，85 个测试；这次通道正常，抓到了一份真实的成功事件流当夹具 |
| opencode | 启动参数、事件流解析、结局判定 | 通过，40 个测试 |

- opencode 路质疑规格里的附件参数在调研报告的参数表里查不到。参数本身存在（主会话早先看过帮助输出），但它是「数组」类型，按规格里写的顺序会把紧跟其后的任务正文也吞成附件。**这是主会话写规格时的错误**，记入评审清单交给修复路。
- 主会话补写运行时注册表（14 行接线代码，按计划由主会话写）。
- 全量门禁：33 个测试文件、347 个测试全部通过，构建通过。
- 主会话介入业务代码次数：0

## 当前调度表

| 路 | 角色 | 在干什么 | 状态 |
|---|---|---|---|
| — | — | M0 全部实现交回，等待独立评审 | — |

## 下一步

M0 提交；派两路只读评审分别审核心规则和运行时适配；同时主会话写 M1 服务层与命令行的模块设计。
