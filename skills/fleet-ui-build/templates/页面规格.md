<!--
  页面层规格模板。每页一份，放在 design/<页面>.md，文件名用页面名（首页.md、报名.md……）。
  用法：站点层写完、地基路派出之后，主控逐页照这份模板写。所有页由同一个人依次写完，不拆给别人。
  规矩：
  1. HTML 注释是填写说明，填完删掉。
  2. 每个区块七项都要写：外层、每个元素、数据来源、交互、状态、响应式、文案。缺哪项，苦工就在哪项上自由发挥。
  3. 数字、类名、文案直接写成最终值。「一句介绍」「适当留白」等于没写。
  4. 业务数字一律写成「从哪个数据、用哪个格式化函数取」，不在这里写死数值（写死会和数据层对不上）。
  5. 这一页要被交互检查找到的元素，写明 data-* / id / 按钮文字，并在末尾的表里汇总。
  6. 写完对照 references/规格怎么写满.md 末尾的自查。
-->

# <页面名> `<路由>` —— 逐区块规格（DESIGN.md 第 5 章之一）

> 先读完 `DESIGN.md` 全文再读本文件。令牌、共享组件签名、文案原则、写入边界都在 DESIGN.md，这里不重复。
> 规格里写了的元素一个都不能少；没写的细节按同一套令牌补齐，不要自己加区块、加文案。
> 凡是写了 `data-*` 属性、`id`、`aria-label` 的地方，交互检查会用到，**名字和取值不能改**。

<!-- 下面三行各写一句，苦工靠它们理解这一页的轻重。 -->
本页节奏：<从上到下每个区块的底色，如 深色首屏 → 白底 → 冷灰 → 白底 → 深色页脚>。
本页核心动作：<首屏必须能做的事>。
本页最重要的区块：<编号与名称>，允许写得最长、视觉投入最大。

---

## <编号> <区块名> `<文件路径>`（服务端组件 / `"use client"`）

<!-- 交互最重的区块可以允许拆子组件：写明只能新建为 <区块名>-*.tsx，状态放在主文件往下传。 -->

**外层**：<元素类型、完整类名、锚点 id、底色、容器、纵向留白>。

**元素**：<从上到下、从左到右逐个写：是什么、放什么内容、完整类名或字号颜色、怎么排>
1. 
2. 

**数据**：<从哪个导出取，用哪个格式化函数显示；期望显示成什么，如「显示 9 月 28 日 18:00」>。

**交互**：<状态存什么形状、默认值；点了怎样；边界条件：选满禁用、点第二次取消、切换后哪些状态要重置>。

**状态**：<空态、缺值、加载、错误、超限各显示什么，写最终文案；本次数据不会触发的状态也要写>。

**响应式**：<哪个断点换成哪套结构；宽表格与宽图放进横向滚动容器，外层网格子项加 min-w-0；小徽标 self-start；左右两栏写明等高还是顶部对齐>。

**文案**：<所有出现的字，照最终文案写；区块只放标题，不配副标题>。

---

## 本页交互检查会找的东西

<!--
  交互检查（tools/probe.mjs）按这张表写断言。每条写具体的值——「187」能验出公式算错，「非空」验不出。
  期望值能从数据层单测算出来的，一律从单测来。
-->

| 位置 | 操作 | 断言 |
|---|---|---|
| | | |
