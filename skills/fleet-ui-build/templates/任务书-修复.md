<!--
  修复任务书模板。验收发现的问题攒成一份派一路，不要边看边自己改。
  关键：每条缺陷写清「在哪、现在是什么、改成什么、为什么」。
  只写「为什么」不写「改成什么」，苦工会顺手重构；只写「改成什么」不写「为什么」，
  它会在别处犯同类错误。两个都写，改动才既准确又不扩散。
  同类问题可能散在别的文件（比如网格子项缺 min-w-0），就在那一条里要求全站排查，并限定只准加哪一个类。
-->

你在 `<项目绝对路径>` 工作（<技术栈>）。

【背景】<页面名或全站> 已实现，类型检查、构建、交互检查都通过。主控看截图和读代码后发现 <N> 处缺陷，**只修这 N 处，其它一律别动**：不顺手重构、不改文案、不调其它样式、不加依赖。

--- 缺陷 1：<一句话现象> ---
文件：`<路径>`
<现象描述：在什么宽度、什么条件下、看起来怎么样、为什么是错的>
改法：<具体到改哪个类名、加哪一行、位置在哪个元素的内部还是之后>。<只加这一个类 / 只改这一处>。
<需要时：用 grep 把全站同类写法过一遍，凡是 <条件> 的，同样只加 <这个类>，改了哪些在回报里列出。>

--- 缺陷 2：<一句话现象> ---
文件：`<路径>`
<同上>

【你能改的文件，只有这几个】
<逐行列出；全站排查允许改的范围也写明>

【不准碰】其它任何文件，尤其数据目录、共享组件、页面文件、DESIGN.md、design/。

【自检】改完跑 `<类型检查命令>`，必须零错误。不要跑构建，不要起开发服务器，不要 git 操作。

【回报格式，最后原样输出这五段】
SUMMARY: 每处各改了什么
FIXED: 每条一行「缺陷编号 — 改了哪个文件的哪里 — 怎么验证的」；全站排查改到的位置也列在这里
REJECTED: 你认为不成立的缺陷，写明编号和理由，没有就写「无」
NEW_ISSUES: 修的过程中发现的其他问题，只记录不处理，没有就写「无」
SELF_REPORT: pass 或 fail
