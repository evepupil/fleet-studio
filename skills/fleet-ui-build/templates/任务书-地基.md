<!--
  地基路任务书模板。这一路只搭地基，不实现任何区块。
  它只依赖站点层（DESIGN.md），所以站点层写完就能派，页面层由主控同时往下写。
  它的产物是后面所有并发路的共同依赖，主控必须核对后才放行。
  <尖括号> 里的内容按项目替换；HTML 注释填完删掉。
-->

你在 `<项目绝对路径>` 工作。目录里现在已有：DESIGN.md、<已就位的依赖清单与锁文件>、<主控已写好的数据目录与单测（如有）>、<素材目录>、.fleet/。

【第一步】完整读一遍 `<项目绝对路径>\DESIGN.md`（站点层规格），重点是第 1 章技术栈与图标白名单、第 2 章令牌、第 3 章动效、第 4 章文件结构与共享组件签名、第 7 章门禁、第 8 章写入边界。`design/` 目录是页面层规格，可能还在写，**你不用读**。

【你的目标】
搭好地基：构建配置、设计令牌、全局样式、共享组件、每页骨架、全部区块占位。
**只做地基，不实现任何区块的真实内容**，区块由后面 N 个并发会话实现。

【第二步：脚手架】
<!-- 主控可以先把依赖装好再派地基路，这里就写「依赖已装好，不要再装」。 -->
1. 从 `<已有同栈项目路径>` 复制这些文件到本项目根：<依赖清单、锁文件、样式与类型配置、lint 配置、忽略规则>。
2. 把依赖清单的 name 改成 `<项目名>`。
3. lint 配置里关掉「站内链接必须用框架链接组件」和「必须用框架图片组件」两条规则（本项目一律用原生 `<a>` 和 `<img>`）。
4. 安装依赖：先试 `<包管理器> install --offline`（本机缓存里有就 20 秒装完）；失败再用 `--prefer-offline`。不要换包管理器。
5. <构建配置文件的具体内容，多页静态导出写明 trailingSlash>
6. 类型配置确认 strict、noUnusedLocals、路径别名。

【第三步：公共文件，全部由你写】
1) `<样式配置文件>` —— 色名与色值严格按 DESIGN.md 2.1；字族按 2.2；阴影按 2.3。<可以直接给出逐字内容>
2) `<全局样式文件>` —— <按 2.3、2.4 列出要定义的工具类和全局规则>
3) `<类名合并函数文件>`
4) 数据：<二选一> 数据目录已由主控写好并通过单测，**一个字都不要改** / 逐字复制 DESIGN.md 附录的代码块，不要自己发挥，不要改字段名。
5) `<根布局文件>` —— <语言、字体、标题模板；多页站的顶栏页脚不放在这里>
6) 每页一个页面文件，按 DESIGN.md 4.2 的顺序 import 并渲染本页区块：<逐页列出路由、文件、标题、当前页参数>
7) 全部区块占位（**每个都必须建，内容极简**）：
   <逐页逐个列出 路径 → 导出名>
   每个文件就是一个渲染了区块名的空容器，带上规格里的锚点 id，例如：
     export function Hero() { return <section id="top" className="py-16 text-center">Hero</section>; }
     export default Hero;
   **必须同时提供具名导出和默认导出**，组件名一个字母都不能错。需要「当前页」参数的顶栏，占位就把参数签名写好。

【第四步：共享组件，签名严格按 DESIGN.md 4.3】
<!--
  这几个组件是全页视觉一致的保证：各区块 import 同一套，就不会各写各的胶囊、各写各的数值渲染。
  每个都要给出完整签名和默认类名，苦工才不会自己设计一套。
-->
- 样式拼接函数（按钮、胶囊、输入框的类名函数）单独放一个**不带客户端指令**的文件——服务端组件也要调用它们。
- 只有用到状态、副作用或事件处理的组件第一行写客户端指令，其余不写。
- 按参数切换颜色的组件，一律用对象映射完整类名，禁止拼接。
- <逐个列出组件文件、导出名、要点>

【第五步：验证】
依次执行，必须全部通过：
```
<单测命令（如有）>
<类型检查命令>
<构建命令>
```
构建后确认每一页的导出文件都存在：<逐个列出>。失败就自己修到通过为止（常见坑：lint 的未转义引号、未使用的 import、样式配置的扫描路径、服务端组件调用了客户端文件导出的函数）。

【禁止】
- 不准改 DESIGN.md 与 design/
- 不准实现任何区块的真实内容
- 不准动 <数据目录（主控已写好时）>、<素材目录>
- 不准装规格之外的包、不准启动开发服务器、不准 git 操作

【回报格式，最后原样输出五行】
SUMMARY: 一两句话说清做了什么
FILES: 新建的文件按目录汇总
VERIFY: 单测、类型检查和构建的实际执行结果
SELF_REPORT: pass 或 fail
BLOCKED: 卡住的点，没有就写「无」
