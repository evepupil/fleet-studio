<!--
  站点层规格模板。用法：复制到项目根，改名 DESIGN.md，逐章填满。
  每一页的逐区块规格另写在 design/<页面>.md（模板见 页面规格.md），本文件第 5 章只放链接。
  规矩：
  1. 这些 HTML 注释是填写说明，填完要删掉，不要留给苦工。
  2. 每一章都要填。填不出来的写「无」并说明为什么，空着等于没做。
  3. 凡是能写成数字的都写成数字。「偏冷的克制风格」等于没写。
  4. 站点层写完就可以派地基路；页面层和地基同时进行。
  5. 单页站同样用这份模板，页面清单只有一行，第 5 章只链一个文件。
-->

# <产品名> —— 站点层规格（唯一事实来源）

> 所有实现会话动手前先读完本文件，再读 `design/` 下属于自己那一页的规格。本文件和 `design/*.md` 是契约，**实现会话不得修改**。
> 这份规格故意写满：这类站该有的东西全部先摆上，数据先编。删减是主控验收时的事，实现会话只负责按规格做出来。
> 规格分两层：本文件定全站的东西，**每路必读全文**；`design/<页面>.md` 写逐区块规格，**每路只读自己那一页**。

---

## 0. 定位与参照

<!--
  三段：这是什么站、用户来干什么、目标观感。
  然后是参照表——全文最省力的一章。点名 2~4 个真实站点，模型认识它们，一个名字顶一百句形容词。
  每行写清抄什么、不抄什么。没有参照就先联网找，或从记忆里点名同类里做得好的。禁止只写「现代简洁」。
  打不开的参照如实写「未能访问」，凭记忆的写「凭记忆」。
-->

<一句话说清这是什么站>

用户来这里干这几件事：<列 3~5 条具体动作>

目标观感：**<一句话>**。参照对象和各自抄什么：

| 参照 | 抄什么 |
|---|---|
| <站点名 + 网址> | <具体到区块和信息关系> |

不抄：<列出这类参照里不要的东西，如广告位、评分、玻璃拟态、重复出现的赞助商墙>

### 0.1 页面清单

<!--
  站点层的核心。先把页面定下来，页面层才有地方落。
  「首屏核心动作」写这一页打开第一眼必须能做的事；「区块下限」按参照站对应页面的区块数定，不能更少。
  「分路」写这一页的区块分给哪几路苦工（与第 8 章分路表一致）。
-->

| 页面 | 路由 | 用户来干什么 | 首屏核心动作 | 参照页 | 区块下限 | 分路 |
|---|---|---|---|---|---|---|
| <首页> | `/` | | | | | |

---

## 1. 技术栈（已锁定）

<!--
  锁死，苦工不准换。特别注意：
  - 图标库要给「可用图标白名单」。苦工会为了确认某个图标存不存在去翻依赖目录，烧掉半条命。
    白名单要先在本机已装的版本里逐个确认存在。
  - 明确点名禁止的依赖，否则会冒出动画库、UI 框架、图表库、地图库。
  - 多页静态导出写明 trailingSlash，站内链接一律写成带尾斜杠的绝对路径。
-->

| 项 | 值 |
|---|---|
| 框架 | <> |
| 语言 | TypeScript，`strict: true` + `noUnusedLocals: true` |
| 样式 | <> |
| 图标 | <> |
| 图表 | <手写 SVG / 其他> |
| 单测 | <> |
| 包管理 | <> |

**不引入**：<逐个点名>。

可用图标白名单（已确认存在，只用这些）：<逐个列出>。需要别的形状用内联 SVG 手写。

---

## 2. 设计令牌

### 2.1 颜色

<!-- 每个色名给十六进制值和用途。正文色、次级文字色要标对比度。写清哪些颜色只能做图形、不能做文字。 -->

| 类名 | 色值 | 用途 |
|---|---|---|
| | | |

**组件里禁止写十六进制色值**（例外写在这里，比如 SVG 渐变的 stopColor）。

### 2.2 字阶与排版

<!-- 字族、每一级标题的完整类名串（含响应式断点）、正文、辅助文字。数字用什么字体、怎么对齐。 -->

### 2.3 间距、圆角、阴影、断点

<!-- 容器宽度、区块纵向留白、圆角档位、阴影完整值、焦点样式、验收看的两档宽度。 -->

### 2.4 全局样式

<!-- 写进全局样式文件、各区块直接用的类。 -->

---

## 3. 动效纪律

<!--
  这一章定「该不该动」，不同类型的站答案相反：
  - 营销页：入场、视差、数字滚动、光斑跟随都可以，但只动 transform 和 opacity。
  - 资料站 / 工具站：明确写「不做滚动入场、不做数字滚动、不做无限循环动画」，只留 hover 和焦点态。
  - 混合站：逐条写可以动的（hover、手风琴、抽屉、倒计时换字）和不准动的，加载转圈是唯一的循环例外。
  这条不写，苦工会按营销页的默认审美加一堆动效。
-->

---

## 4. 文件结构与共享组件

### 4.1 路由与页面骨架

<!--
  每页的路由、文件、标题。写清全站共用的顶栏页脚放在哪里：
  需要高亮当前页的顶栏由每个页面文件渲染并传「当前页」参数——这是「区块不接收参数」的唯一例外，写明。
-->

| 路由 | 文件 | 标题 |
|---|---|---|
| `/` | | |

每个区块组件**具名导出 + 默认导出双份**，不接收参数（顶栏除外），自带最外层容器、锚点 `id`、底色和纵向留白。

### 4.2 区块清单

<!-- 按页分表。这张表就是第 8 章分路和门禁导出名核对的依据。每页标出最重要的区块。 -->

<页面名>（规格见 `design/<页面>.md`）：

| # | 区块 | 组件 | 文件 | 锚点 | 底色 |
|---|---|---|---|---|---|
| | | | | | |

### 4.3 共享组件签名

<!--
  地基路照这一节写共享组件，各页区块直接 import。每个组件写：文件、导出名、完整参数类型、默认类名、各变体的完整类名串。
  这是全站视觉一致的保证：各页 import 同一套，就不会各写各的按钮、胶囊、数值渲染。
  两条硬规矩写进来：
  - 样式拼接函数单独放一个不带客户端指令的文件（服务端组件也要调用它）；
  - 按状态切换颜色一律对象映射完整类名。
-->

| 文件 | 导出 | 签名与样式 |
|---|---|---|
| | | |

---

## 5. 各区块详细规格

按页拆在 `design/` 下，每个区块写七项：外层、每个元素、数据来源、交互、状态、响应式、文案。

- [design/<页面>.md](design/<页面>.md) —— <区块编号范围>

---

## 6. 文案原则

<!-- 语言、句长、数字要具体、禁止开发备注和占位符、禁止空洞状态文案、链接文字写去向、必须保留的声明句。用户的界面文案守则抄进来。 -->

---

## 7. 质量门禁

```bash
<单测命令>
<类型检查命令>
<构建命令>
<确认每一页的导出文件都在>
```

自查：<逐条列出，如客户端指令、禁止 any、禁止未使用 import、禁止取当前时间与随机数、组件里不写死业务数字、移动端不溢出、图片有 alt、SVG 有 role 与 aria-label、可交互元素有焦点态>

---

## 8. 文件写入边界与分路表（并发协作铁律）

<!-- 这一章是零冲突的保证。公共文件列全；分路表按页面目录切，交互最重的区块单独一路。 -->

- 公共文件：<逐个列出>。**区块实现会话一律不准碰。**
- 每路只写分给自己的文件；要拆子组件，只能放在本页目录、以自己的区块名开头。
- 只能 import：<白名单>。**不 import 其他区块的文件。**

| 路 | 负责的文件 |
|---|---|
| L1 | |

---

## 9. 哪些是编的

<!--
  给用户的替换清单，不是待确认问题表。逐项标真实 / 编造，真实的写清出处，编的写清合理性依据。
  有单测校验的一致性（比如名额构成之和等于总名额）也写上。
-->

| 项 | 状态 |
|---|---|
| | **真实**，来自 <出处> |
| | **编的**，<说明合理性依据> |

---

## 附录 数据

<!--
  二选一：
  A. 数据少（几十条以内）且没有计算逻辑：直接把 lib/data.ts、lib/format.ts 写成代码块，地基路逐字落盘，主控 diff 核对。
  B. 数据多或带计算（生成器、校验规则、推算公式）：主控直接写成 lib/*.ts 并配单测，这里只放接口索引——
     每个文件列出导出名和一句话用途。交互检查要核对的具体数字从单测里算出来。
  跟时间有关的状态（倒计时、报名中 / 已截止），在数据里定一个演示用的固定当前时刻。
-->
