<!--
  单路区块任务书的后半段。拼在「任务书-公共.md」之后。
  篇幅参考：每路 3~7 个区块，后半段 20~40 行。
  补充要求写什么：页面层规格已经写过的不要重复，这里只写
  「跨区块的协调」「容易做错的地方」「规格里有歧义的地方」「交互检查会找哪些标记」。
-->

【本次任务】实现 <页面名> 的区块 <编号>「<名称>」、区块 <编号>「<名称>」……，严格照页面层规格对应小节。
<!-- 如果其中某块是本页最重要的，在这里点明，并允许它写得最长。 -->

【你负责的页面规格】`design/<页面>.md`（只看属于你的小节和文末的交互检查表）

【你独占的文件】
<路径> —— 覆盖现有占位，导出 <组件名>（具名 + 默认）
<路径前缀>-*.tsx —— 可选，新建的子组件，只能用这个前缀、放在本页目录

【补充要求】
<!--
  按区块分条。每条回答一个具体问题，不要复述规格。典型内容：
  - 哪个文件需要客户端指令，状态存什么形状、默认值、切换时重置哪些
  - 交互的具体判定：点第二次取消、选满几个禁用、空结果时按钮文字换成什么
  - 数据从哪个导出来、用哪个格式化函数
  - 交互检查会找的 data-* / id / 按钮文字，再点一遍名
  - 输入框、下拉必须是受控组件（交互检查用原型上的 value setter 赋值再派发 input / change 事件）
  - 位置类指令写清是容器内还是容器外，「紧后面」有歧义
-->
- <区块>：<具体要求>
- <跨区块的一致性要求>
